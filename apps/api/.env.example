PORT=4000
LIGHTWATCH_API_BASE_URL=http://localhost:3003
LIGHTWATCH_REALTIME_URL=http://localhost:3002
LIGHTWATCH_API_KEY=
REQUEST_TIMEOUT_MS=10000
NODE_ENV=development

# Auth (set AUTH_ENABLED=true to require login)
AUTH_ENABLED=false
SESSION_SECRET=change-me-to-a-random-string
CONSOLE_USERNAME=admin
# Generate: node -e "console.log(require('crypto').createHash('sha256').update('your-password').digest('hex'))"
CONSOLE_PASSWORD_HASH=
