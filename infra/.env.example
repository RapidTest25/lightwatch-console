# Lightwatch Console — Docker environment
# Copy to .env and fill in values

LIGHTWATCH_API_BASE_URL=http://host.docker.internal:3001
LIGHTWATCH_API_KEY=your-lightwatch-api-key
SESSION_SECRET=change-me-to-a-random-64-char-string
CONSOLE_USERNAME=admin
# Generate with: node -e "const c=require('crypto');console.log(c.createHash('sha256').update('yourpassword').digest('hex'))"
CONSOLE_PASSWORD_HASH=put-sha256-hash-here
